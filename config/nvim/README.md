# nvim

Minimal Neovim config for editing inside a container, with yank wired to the host clipboard over OSC52.

See [docs/nvim.md](../../docs/nvim.md) for how it works, where it works, and requirements.
